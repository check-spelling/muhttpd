CFLAGS += 
LIBS += 

VERSION = 1.0.5

PREFIX ?= $(DESTDIR)/usr/local
PACKAGEDIR ?= $(PREFIX)/share/$(PACKAGE)-$(VERSION)
TRUEPREFIX ?= /usr/local
TRUEPACKAGEDIR ?= $(TRUEPREFIX)/share/$(PACKAGE)-$(VERSION)
SBINDIR ?= $(PREFIX)/sbin
SYSCONFDIR ?= $(DESTDIR)/etc
MANDIR ?= $(PREFIX)/man
LOGDIR ?= $(DESTDIR)/var/log

OBJECTS = main.o init.o socket.o request.o status.o config.o globals.o strndup.o clearenv.o type.o handler.o log.o
